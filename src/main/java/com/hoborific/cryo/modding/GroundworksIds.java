package com.hoborific.cryo.modding;

import net.minecraft.util.Identifier;

/**
 * Identifiers for block and items added by this mod.
 */
class GroundworksIds {
    // Misc. identifiers not handled elsewhere.
    static final Identifier CHALK =
            new Identifier(GroundworksInitializer.MOD_ID, "block_chalk");

    /**
     * Do not instantiate.
     */
    private GroundworksIds() {
    }
}
